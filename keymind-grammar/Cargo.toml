[package]
name = "keymind-grammar"
version = "0.1.0"
edition = "2021"
description = "LanguageTool wrapper for real-time sentence grammar checking and text fixing in Rust."
license = "MIT OR Apache-2.0"

[dependencies]
reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1.38", features = ["sync", "rt-multi-thread", "macros", "time", "process"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
lru = "0.12"
fnv = "1.0"
arboard = "3.4"
thiserror = "1.0"
parking_lot = "0.12"
tracing = "0.1"
async-trait = "0.1"

[dev-dependencies]
tokio = { version = "1.38", features = ["full"] }

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.52", features = ["Win32_UI_Input_KeyboardAndMouse", "Win32_Foundation"] }
